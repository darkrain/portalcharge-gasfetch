{
  "name": "portalcharge-gasfetch",
  "version": "1.0.0",
  "description": "Request gas for create transaction",
  "main": "index.js",
  "scripts": {
    "createbin": "node --experimental-sea-config sea-config.json && cp $(command -v node) index && npx postject index NODE_SEA_BLOB gasfetch.blob --sentinel-fuse NODE_SEA_FUSE_fce680ab2cc467b6e072b8b5df1996b2 && rm gasfetch.blob && mkdir -p bin && mv index bin/portalcharge-gasfetch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ethers": "^6.13.4",
    "express": "^4.21.1"
  }
}
